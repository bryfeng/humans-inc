# Ignore platform checks for optional dependencies
# This allows lightningcss to install the correct platform-specific binary
# based on the build environment rather than the development environment
target_platform=
target_arch=
target_libc=

# Treat optional dependencies as non-blocking
optional=false
