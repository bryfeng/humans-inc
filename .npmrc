# Allow platform-specific optional dependencies to install based on current platform
# This ensures the correct lightningcss binary is installed for each environment
target_platform=
target_arch=
target_libc=

# Include optional dependencies (default behavior, but being explicit)
include=optional

# Don't fail build if optional dependencies can't be installed
optional=true